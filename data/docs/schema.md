bq query --use_legacy_sql=false \
'CREATE TABLE `project-7b250e67-6e23-4c02-ab5.openrouter.openrouter_traces` (
  `trace_id` STRING NOT NULL,
  `span_id` STRING NOT NULL,
  `parent_span_id` STRING,
  `user_id` STRING,
  `session_id` STRING,
  `organization_id` STRING,
  `entity_id` STRING NOT NULL,
  `api_key_name` STRING,
  `timestamp` TIMESTAMP NOT NULL,
  `start_time` TIMESTAMP NOT NULL,
  `end_time` TIMESTAMP,
  `duration_ms` FLOAT64,
  `model` STRING,
  `provider_name` STRING,
  `provider_slug` STRING,
  `status` STRING NOT NULL,
  `level` STRING NOT NULL,
  `finish_reason` STRING,
  `prompt_tokens` INT64 NOT NULL,
  `completion_tokens` INT64 NOT NULL,
  `total_tokens` INT64 NOT NULL,
  `cached_tokens` INT64 NOT NULL,
  `reasoning_tokens` INT64 NOT NULL,
  `input_cost` FLOAT64 NOT NULL,
  `output_cost` FLOAT64 NOT NULL,
  `total_cost` FLOAT64 NOT NULL,
  `input_unit_price` FLOAT64,
  `output_unit_price` FLOAT64,
  `span_type` STRING NOT NULL,
  `span_kind` STRING NOT NULL,
  `operation_name` STRING NOT NULL,
  `attributes` JSON,
  `input` JSON,
  `output` JSON,
  `metadata` JSON,
  `tags` ARRAY<STRING>,
  `model_parameters` JSON,
  `resource_attributes` JSON,
  `schema_version` INT64 DEFAULT 1
);'